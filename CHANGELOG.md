# Changelog

### Release Notes

#### 1.2.0

- Added useRule setting — @use instead of @import due to @import deprication 
- Added newFileTemplate setting — array of lines prepended to every new partial file created via the New Partial File command. Usefull for prepending @use rules

